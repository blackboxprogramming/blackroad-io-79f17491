name: CI Failure Tracker

on:
  workflow_run:
    workflows: ["CORE CI"]
    types: [completed]

permissions:
  contents: read
  issues: write

jobs:
  report:
    uses: ./.github/workflows/reusable/failure-tracker.yml
    permissions:
      contents: read
      issues: write
