name: Auto Label

on:
  pull_request:
    types: [opened]

permissions:
  contents: read
  pull-requests: write

jobs:
  label:
    uses: ./.github/workflows/reusable/auto-label.yml
    permissions:
      contents: read
      pull-requests: write
